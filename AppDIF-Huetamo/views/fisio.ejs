<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="./css/styles.css">
    <title>vista Administrador</title>
</head>
<body>
    <%- include('./partials/_header.ejs') %>

    <div class="container mt-5 mb-5">
        <div class="table-responsive">
            <table class="table table-bordered table-hover">
                <h1 class="mb-5 text-center">Citas del fisioterapeuta</h1>
                <thead class="table-dark">
                    <tr>
                        <th scope="col">No</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Apellido paterno</th>
                        <th scope="col">Apellido materno</th>
                        <th scope="col">Fecha de Nacimiento</th>
                        <th scope="col">Fecha de Cita</th>
                        <th scope="col">Hora de Cita</th>
                        <th scope="col">Motivo</th>
                        <th scope="col">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (data && data.length > 0) { %>
                        <% for (let i = 0; i < data.length; i++) { %>
                    <tr>
                        <td><%= i +1 %></td>
                        <td><%= data[i].nombre %></td>
                        <td><%= data[i].apellido_paterno %></td>
                        <td><%= data[i].apellido_materno %></td>
                        <td><%= data[i].fecha_nacimiento %></td>
                        <td><%= data[i].fecha_cita %></td>
                        <td><%= data[i].hora_cita %></td>
                        <td><%= data[i].motivo %></td>
                        <td>
                            <a href="/eliminarA/<%= data[i].id %>" class="btn btn-danger btn-sm btn-custom">Eliminar</a>
                            <a href="consultarA/<%= data[i].id %>" class="btn btn-primary btn-sm btn-custom">Modificar</a>
                        </td>
                    </tr>
                            <% } %>
                        <% } else { %>
                            <tr>
                                <td colspan="9" class="text-center">No hay citas disponibles</td>
                            </tr>
                        <% } %>
                    </tbody>
                </tbody>
            </table>
        </div>
        
    </div>
    



    <%- include('./partials/_footer.ejs') %>
</body>
</html>